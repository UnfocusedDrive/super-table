import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';

const App = () => {
  return (
    <div>
      <button>Your build is successful.</button>
    </div>
  );
}

ReactDOM.render(
  <App />,
  document.body
);
